# Where we tell Grunt what to do when we type "grunt" into the terminal
# $ grunt
serve:
  description: 'Running local server'
  tasks: 
    - 'connect'
    
default:
  description: 'Just Building stuff'
  tasks:
    - 'env:dev'
    - 'concurrent:dev'
